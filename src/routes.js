/**
 * Faceless Blur site routes (must match live + local).
 *   /          → Root Directory hub
 *   /hospital  → CSV Hospital tool
 */
export const ROUTES = {
  ROOT: '/',
  HOSPITAL: '/hospital',
}

export default ROUTES
